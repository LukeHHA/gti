// The value and indexed core is implemented without pointer decay or hidden
// allocation. Iteration remains pending an owner-dependent fixed-array view.
namespace std {
class array<T, uint64_t N> {
  mut T values[N];

public:
  array() : values({}) {}

  array(T initial[N]) : values(initial) {}

  size_t size() {
    return N;
  }

  bool empty() {
    return N == 0;
  }

  T& at(int index) {
    return this.values[index];
  }

  mut T& at(int index) mut {
    return this.values[index];
  }

  T& at(size_t index) {
    return this.values[index];
  }

  mut T& at(size_t index) mut {
    return this.values[index];
  }

  T& front() {
    return this.values[0];
  }

  mut T& front() mut {
    return this.values[0];
  }

  T& back() {
    return this.values[N - 1];
  }

  mut T& back() mut {
    return this.values[N - 1];
  }

  T& operator[](int index) {
    return this.values[index];
  }

  mut T& operator[](int index) mut {
    return this.values[index];
  }

  T& operator[](size_t index) {
    return this.values[index];
  }

  mut T& operator[](size_t index) mut {
    return this.values[index];
  }

  void fill(T value) mut
    requires std::copyable<T> {
    for (mut size_t index = 0; index < N; index++) {
      this.values[index] = value;
    }
  }

  void swap(mut array<T, N>& other) mut
    requires std::movable<T>;
};
}
