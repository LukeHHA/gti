#include <std/string>
#include <std/vector>

int main(int argc, std::vector<std::string> argv) {
  if (argc != 1 or argv.size() != std::size_t(1)) {
    return 1;
  }
  std::println("program-arguments-ok");
  return 0;
}
