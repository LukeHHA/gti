// Dependencies used by this file remain private to this source unit.
include "range_detail.gti"

int clamp(int value, int minimum, int maximum) {
  int lower_bounded = clamp_lower(value, minimum);
  return clamp_upper(lower_bounded, maximum);
}
