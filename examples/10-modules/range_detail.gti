int clamp_lower(int value, int minimum) {
  if (value < minimum) {
    return minimum;
  }
  return value;
}

int clamp_upper(int value, int maximum) {
  if (value > maximum) {
    return maximum;
  }
  return value;
}
